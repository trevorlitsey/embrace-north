{
  "name": "embrace-north",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "deploy": "./deploy.sh",
    "build:client": "cd client && npm run build",
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev"
  }
}
